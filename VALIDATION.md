# 검증 기록

## 확인한 범위

Node.js에서 실제 앱의 데이터 처리 모듈과 HTML/DOM 이벤트 흐름을 실행하여 **82개 검사**를 통과했습니다. DOM 환경은 linkedom, 이미지 생성 검사는 네이티브 Canvas 구현을 사용했습니다.

- 별 15개 제한, 자기 자신·중복 연결 방지, 잘못된 저장 데이터 정리
- 단계별 활동, 별 추가·이동·연속 연결, 연결선 좌표 갱신
- 별·연결선 삭제, 실행 취소·다시 실행, 취소된 드래그 복구
- 크기·밝기 조절, 스케치·지우개와 실행 취소
- 별 4개의 연결 여부, 상상 질문, 필수 입력과 선택형 이야기
- 저장 상태 복원, 저장 제한 환경에서 계속 작업
- 완성 화면에 이미지 저장 버튼만 표시, 상단 단계로 돌아가서 수정
- 친구 맞히기·정답 공개·발표 기능 제거 확인
- 초기화 확인·취소, 입력 내용의 HTML 실행 방지
- 실제 PNG 생성 버튼의 출력과 1600 × 1000 크기, 다운로드 보조 이미지

Noto Sans KR 글꼴을 등록하여 한글 PNG를 생성하고 이미지를 눈으로 확인했습니다. 최대 길이의 한글 이름·설명·이야기도 작품 그림 영역과 겹치지 않는지 확인했습니다. 상대 경로 자산 및 ZIP 무결성을 검사했습니다.

## 아직 확인하지 않은 범위

실제 PC Chrome·갤럭시탭 Chrome에서의 시각적 배치, 손가락 터치, 브라우저 다운로드 동작은 이 제작 환경에서 직접 검사하지 못했습니다. DOM 이벤트 검사와 실제 브라우저 검사는 다릅니다.

## 수업 전 기기 확인 (약 2분)

1. 사용할 태블릿에서 배포한 Pages 주소를 엽니다.
2. 별 4개를 놓고 한 별을 손가락으로 끌어 움직입니다.
3. 별을 차례로 이어 보고 한 번 실행 취소·다시 실행합니다.
4. 떠오른 모습, 이름, 번호를 쓰고 완성합니다.
5. 완성 화면에서 이름과 설명이 표시되고 이미지 저장 버튼만 있는지 확인합니다.
6. PNG를 저장하여 다운로드 항목에서 열고 한글을 확인합니다.
7. 새로고침 후 작품이 유지되는지 확인합니다.

PNG 자동 다운로드가 막히면 저장 후 나타나는 보조 이미지를 길게 눌러 저장할 수 있습니다.
